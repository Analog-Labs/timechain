version: "3.7"

services:
  dev:
    container_name: timechain-node
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/timechain-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/timechain-node/.cargo
    volumes:
      - .:/var/www/timechain-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/timechain-node --dev --ws-external"
