type Mutation {
  collect(
    collection: String!
    taskId: Int!
    taskCounter: Int!
    block: Int!
    cycle: Int!
    tss: String!
    data: [String!]!
  ): CollectResult!
}

type CollectResult {
  status: String!
}