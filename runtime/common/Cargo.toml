[package]
name = "runtime-common"
version = "1.0.0"
authors = [ "Analog Devs <https://github.com/Analog-Labs>" ]
edition = "2021"
license = "LGPL-3.0"
homepage = ""
repository = ""

[dependencies]
sp-core = { version = "7.0.0", default-features = false, git = "https://github.com/paritytech/substrate.git", branch = "polkadot-v0.9.42" }
sp-weights = { version = "4.0.0-dev", default-features = false, git = "https://github.com/paritytech/substrate.git", branch = "polkadot-v0.9.42" }

[features]
default = [ "std" ]
std = [
    "sp-core/std",
    "sp-weights/std",
]
runtime-benchmarks = [  ]
